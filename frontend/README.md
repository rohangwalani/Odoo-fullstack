# Frontend (React)

Your React frontend will live here.
Once you run `npm create vite@latest . -- --template react` (or your preferred setup) in this folder, the `.gitignore` at the root of the repository will already cover ignoring your `node_modules` and `.env` files.
